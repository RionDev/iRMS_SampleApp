# Vite dev server가 /api/* 를 프록시할 BE 주소 (개별 앱 단독 실행용)
API_PROXY_TARGET=http://irms-be-gateway

# 배포 빌드: scripts/build.sh에서 주입 (개발 시 비워둘 것)
VITE_API_BASE_URL=

# UI 단독 개발 시에만 1로 설정한다. 기본은 실제 sample-service 사용.
VITE_USE_MOCK=
